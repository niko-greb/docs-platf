# ==============================================================
# 📘 Docs-as-Code CI
# Автоматическая проверка документации при Pull Request в main
#
# Что делает:
# - собирает контейнер docs-cli:ci (единая среда линтеров)
# - проверяет только изменённые файлы (Markdown, AsciiDoc, YAML)
# - публикует результаты в комментарий к PR и сохраняет логи
#
# Работает в soft mode — CI не "падает" при ошибках,
# но выводит все предупреждения и ошибки в PR.
# ==============================================================

name: Docs-as-Code CI

on:
  pull_request:
    branches: [ "main" ]  # Проверяем только PR в main

jobs:
  lint-docs:
    runs-on: ubuntu-latest
    name: "Run Docs Linters"

    steps:
      # ---------------------------------------------------------
      # 1️⃣ Клонирование репозитория
      # ---------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # ---------------------------------------------------------
      # 2️⃣ Сборка контейнера с линтерами (Dockerfile лежит в .repo)
      # ---------------------------------------------------------
      - name: Build docs-cli container
        run: docker build -t docs-cli:ci -f .repo/Dockerfile .

      # ---------------------------------------------------------
      # 3️⃣ Определяем изменённые файлы в текущем PR
      # (используется для того, чтобы линтеры проверяли только их)
      # ---------------------------------------------------------
      - name: Detect changed files
        id: changed
        run: |
          echo "CHANGED_FILES=$(git diff --name-only origin/main...HEAD | xargs)" >> "$GITHUB_ENV"

      # ---------------------------------------------------------
      # 4️⃣ Запуск линтеров внутри контейнера
      # Скрипт run-linters.sh проверяет:
      #   - Markdown (markdownlint, mdformat)
      #   - AsciiDoc (asciidoctor)
      #   - OpenAPI (Spectral)
      #   - Style & термины (Vale)
      # Результаты сохраняются в /artifacts/
      # ---------------------------------------------------------
      - name: Run linters inside container
        run: |
          docker run --rm -v "$(pwd):/work" docs-cli:ci \
            bash -lc ".repo/ci/scripts/run-linters.sh"

      # ---------------------------------------------------------
      # 5️⃣ Сохраняем логи линтеров как артефакты CI
      # (можно скачать и изучить при необходимости)
      # ---------------------------------------------------------
      - name: Collect artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: docs-lint-logs
          path: artifacts/

      # ---------------------------------------------------------
      # 6️⃣ Читаем содержимое логов, чтобы вставить их в PR-комментарий
      # (выводим только первые 200 строк каждого файла)
      # ---------------------------------------------------------
      - name: Prepare PR comment (read logs)
        id: read_logs
        run: |
          read_log() {
            if [ -f "$1" ]; then
              head -n 200 "$1"
            else
              echo "⚠️ No log found: $1"
            fi
          }

          echo "Reading markdownlint..."
          md_block="$(read_log artifacts/markdownlint.log)"
          echo "markdownlint<<EOF" >> "$GITHUB_OUTPUT"
          echo "$md_block" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          echo "Reading asciidoc..."
          adoc_block="$(read_log artifacts/asciidoc.log)"
          echo "asciidoc<<EOF" >> "$GITHUB_OUTPUT"
          echo "$adoc_block" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          echo "Reading openapi..."
          openapi_block="$(read_log artifacts/openapi.log)"
          echo "openapi<<EOF" >> "$GITHUB_OUTPUT"
          echo "$openapi_block" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          echo "Reading vale..."
          vale_block="$(read_log artifacts/vale.log)"
          echo "vale<<EOF" >> "$GITHUB_OUTPUT"
          echo "$vale_block" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

      # ---------------------------------------------------------
      # 7️⃣ Публикуем комментарий с результатами линтинга в PR
      #   - markdownlint
      #   - asciidoc
      #   - openapi
      #   - vale
      # Используется sticky-плагин (обновляет старый комментарий,
      # не создавая новый при каждом прогоне)
      # ---------------------------------------------------------
      - name: Comment lint results on PR
        if: always()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: docs-lint
          message: |
            ## 🧩 Docs-as-Code CI Results

            **Markdown**
            ```text
            ${{ steps.read_logs.outputs.markdownlint }}
            ```

            **AsciiDoc**
            ```text
            ${{ steps.read_logs.outputs.asciidoc }}
            ```

            **OpenAPI**
            ```text
            ${{ steps.read_logs.outputs.openapi }}
            ```

            **Vale**
            ```text
            ${{ steps.read_logs.outputs.vale }}
            ```
