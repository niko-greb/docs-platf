=Заголовок без пробела после # # ← ошибка синтаксиса

== Нарушенная таблица
|Поле |Тип
|`id` |integer
| `name` | string  # ← лишние пробелы, нет закрытия

[source,json]
{
  "guid": invalid-uuid-format  # ← не строка, нет кавычек
}

include::not-found.adoc[]  # ← файл не существует

И нет пустой строки в конце