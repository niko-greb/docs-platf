openapi: 3.0.3
info:
title: POS API  # ← нет отступа (ошибка YAML)
version: 1.0.0

paths:
  /order/{guid}:
    post:
      summary: Create order
      parameters:
        - name: guid
          in: path
          required: true
          schema:
            type: string
      responses:
        201:  # ← число вместо строки '201'
          description: Created

  /bad-endpoint:
    get:
      requestBody: {}  # ← GET с телом — недопустимо